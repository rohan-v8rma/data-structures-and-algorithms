#include <iostream>
using namespace::std;

// Replacing every element in an array, with the least greatest element to its right. If there are no greater elements on the right side, replace it with -1.

int main() {
    int arr[10] = {31, 10, 21, 40, 7, 11};

    for(int index = 0;)

    return 0;
}